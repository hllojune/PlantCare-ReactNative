# 팀원 테스트 안내

이 문서는 `PlantCare-ReactNative` 프론트 프로젝트를 팀원이 테스트할 때 필요한 최소 설정만 정리한 문서입니다.

## 1. 전제 조건

백엔드는 한 사람 PC에서만 실행되어 있으면 됩니다.

실행되어 있어야 하는 서버:

- `discovery-service`
- `gateway-service`
- `user-service`
- MySQL

기본 게이트웨이 주소:

- `http://<백엔드PC-IP>:8080`

예시:

- `http://192.168.219.51:8080`

## 2. 같은 네트워크 연결

팀원은 백엔드가 실행 중인 PC와 같은 Wi-Fi 또는 같은 내부 네트워크에 연결되어 있어야 합니다.

## 3. 환경변수 설정

프로젝트 루트에서 `.env` 파일을 만듭니다.

예시:

```env
EXPO_PUBLIC_API_BASE_URL_WEB=http://192.168.219.51:8080
EXPO_PUBLIC_API_BASE_URL_DEVICE=http://192.168.219.51:8080
```

설명:

- 웹 브라우저 테스트도 백엔드 PC IP를 쓰는 방식이 가장 단순합니다.
- 휴대폰 테스트에서는 `localhost`를 쓰면 안 됩니다.

## 4. 프론트 실행

```bash
npm install
npm run web
```

또는 Expo 모바일 테스트:

```bash
npm start
```

## 5. 테스트 항목

### 회원가입

- 아이디 중복 검사
- 닉네임 중복 검사
- 이메일 형식 검사
- 비밀번호 8자 이상 검사

### 로그인

- 존재하지 않는 아이디 에러
- 비밀번호 불일치 에러
- 로그인 성공 시 메인 화면 이동

### 메인 화면

- 로그인한 사용자의 닉네임으로
  - `환영합니다. 호식이님`
  형식의 문구가 보이는지 확인

## 6. 주의사항

- 백엔드 PC IP가 바뀌면 `.env`도 같이 수정해야 합니다.
- 백엔드 PC가 꺼지거나 서버가 내려가면 팀원 테스트도 안 됩니다.
- CORS 문제를 막기 위해 프론트는 게이트웨이 `8080`으로만 요청해야 합니다.
